# number-checker

A simple Python program to check if a number is even or odd.

## Code:

```python
number = int(input("Enter a number: "))

if number % 2 == 0:
    print("Even number")
else:
    print("Odd number")